# 교수님 지적 반영

교수님께서

**N-D(N-Dimensional) 클러스터링**

은 금지하고

**1-D(1-Dimensional) 클러스터링**

은 허용하신 것은, 여러 변수를 복합적으로 고려하여 데이터를 임의로 그룹화(N-D)하지 말고,

**단일 속성(1-D) 기준의 분포나 유사성**

만을 파악하라는 의미로 해석됩니다.

비슷한 컬럼을 하나로 합치는 것에 대해서도 교수님께서 부정적이셨다면, 이는 데이터를 물리적으로 병합하여 정보의 원천(Source)을 모호하게 만들지 말고 **'관계 정의'**에 집중하라는 뜻입니다. 클러스터링 대신 사용할 수 있는 거버넌스 관점의 대안은 다음과 같습니다.

### 1. 1-D 기반의 도메인 프로파일링 (허용된 방식)

여러 지표를 섞지 않고, 특정 하나의 기준(1-D)으로만 컬럼들을 비교합니다.

- **값의 분포(Distribution) 비교**: `column_map.csv`의 `min_val`, `max_val`, `avg_val` 중 **단 하나**의 지표만 선택하여 컬럼들을 정렬해 봅니다.
- **고유 값 일치율 확인**: `distinct_value.csv`를 활용하여 두 컬럼이 공유하는 실제 데이터 값(Value)이 얼마나 일치하는지 단일 기준으로 체크합니다.

### 2. 그래프 이론 기반의 '엔티티 매핑' (합치기 대신 연결하기)

비슷한 컬럼을 합쳐서 없애는 것이 아니라, 이들을 선으로 연결하여

**동치(Equivalent) 관계**

임을 명시하는 전략입니다.

- **연결 고리(Anchor) 설정**: `combined_pair_map.csv`에 있는 `linking_ratio`를 기준으로, 일정 수치 이상의 관계를 가진 컬럼 쌍을 '동일 의미 그룹'으로 선언만 하고 데이터는 각자 유지합니다.
- **표준 용어 매핑**: `table_pair_map.csv`에서 `t_rel_val`이 1(동치)인 경우를 찾아, 서로 다른 시스템의 컬럼들이 '표준 단어(예: 연령)'에 연결되도록 매핑 테이블만 생성합니다.

### 3. 계층 구조(Hierarchy) 및 포함 관계 분석

단순히 "비슷하다"고 묶는 대신, 데이터 간의 상하 관계나 포함 관계를 정의합니다.

- **포함 구조 활용**: `column_domain_tree.csv`나 `table_str_tree.csv`처럼 데이터가 어디서 파생되었고 어디에 속해 있는지 '부모-자식' 관계를 명확히 합니다.
- **As-is vs To-be 관리**: `ERD.pptx`의 사례처럼 원본(Origin)은 그대로 두고, 이를 어떻게 변형(Alter)하여 활용할지에 대한 '이력(Lineage)'을 관리하는 방식입니다.

**요약하자면:**

교수님의 의도는 **"데이터를 뭉뚱그려 합쳐서 정보를 손실시키지 말고(N-D 클러스터링/병합 금지), 단일 지표로 현황을 정확히 파악(1-D 활용)한 뒤 이들이 어떤 관계인지 지도로 그려라"**는 것으로 보입니다. 따라서 '합치기' 보다는 **'표준 매핑 테이블 작성'**을 전략으로 세우시는 것을 권장합니다.

교수님의 피드백 전문을 핵심 내용 위주로 정리해 드립니다.

### 1. 분석의 지향점: BI에서 BR로의 전환

- 단순히 데이터를 시각화하고 현상을 묘사하는 **BI(Business Intelligence)** 수준에 머물지 말고, 데이터가 지켜야 할 BR(Business Rules, 업무 규칙)을 도출하는 데 집중하십시오.
- 이미 알고 있는 지식을 확인하는 것이 아니라, 데이터 사이의 의외성(Unexpectedness)과 숨겨진 인사이트를 찾는 것이 지식 탐사의 본질입니다.

### 2. 기술적 방법론: 통계 묘사 배제 및 알고리즘 최적화

- **수치 기술 지양:** 수치 자체를 묘사하는 것은 큰 의미가 없습니다. 수치 데이터는 **이산화(Discretization)** 과정을 거쳐 범주형 데이터로 변환하여 활용하십시오.
- **엔트로피(Entropy) 활용:** 컬럼별로 엔트로피를 계산하여 정보량을 측정하고, 이를 변수 선택 및 차원 축소의 근거로 삼으십시오.
- **FP-tree 기반 빈발 패턴 탐사:** 빈발 패턴이 있다면 **FP-tree** 알고리즘을 사용하고, 다차원(Multi-dimension) 분석을 통해 다각도의 패턴을 포착하십시오.
- **N-D 정규화 및 클러스터링 경계:** 고차원(N-D)에서의 정규화나 단순 클러스터링은 변별력을 상실할 수 있으므로 주의하고, 필요한 경우 1D 클러스터 대신 이산화를 적용하십시오.
- **Roll-up 효과:** 개념의 계층을 높이는 롤업(Roll-up)을 적용할수록 데이터 패턴이 더 명확하게 보일 수 있습니다.

### 3. 거버넌스 실천 방안: 자동화와 지능화

- **거버넌스 자동화:** 중간에 사람이 개입하여 일일이 판단하는 방식이 아닌, 객관적인 산술 지표에 의해 의사결정이 이루어지는 **지능형 자동화 시스템**을 지향해야 합니다.
- **우선순위(Ranking) 부여:** 분석 결과를 나열만 하지 말고, 사용 빈도나 테이블 간 연결 고리(Connectivity)를 기준으로 **품질 관리 및 표준화의 우선순위**를 수치로 제시하십시오.
- **전체 결합지도 구축:** FK/PK 관계를 활용해 흩어진 테이블들을 하나로 연결한 **전체 결합지도**를 만드는 것은 거버넌스 측면에서 매우 유의미합니다.

### 4. 분석 범위와 목적성

- **Subspace Focusing:** 전체 데이터를 한꺼번에 분석하기보다 시간, 공간, 시공간 등 특정 부분공간(Subspace)을 잘라 분석하고, 그 결과를 대비함으로써 지식의 신뢰도를 높이십시오.
- **목적 중심의 패턴 추출:** 빈발 패턴은 반드시 도메인 관리 및 거버넌스 고도화라는 **분석 목적**을 가지고 수행되어야 하며, 도출된 규칙이 실제 거버넌스 현장에서 몇 퍼센트($\%$)의 신뢰도를 갖는지 입증해야 합니다.

### 5. 기존 자산의 활용

- **ERD에 대한 관점:** 운영계 DB와 달리 데이터 플랫폼의 ERD는 존재하기 어렵거나 쓸모가 없을 수 있습니다. 이미 정밀 지도에 있는 FK/PK 정보를 적극적으로 활용하여 관계를 분석하십시오.

교수님의 피드백과 프로젝트 자료를 바탕으로, **수치 묘사 중심의 BI에서 자동화된 규칙 중심의 BR로 전환하는 지능형 거버넌스 전략**을 정리해 드립니다.

---

### **1. 핵심 전략: BI(Business Intelligence)에서 BR(Business Rules)로 전환**

- **BI (현상 묘사):** 데이터의 현황을 시각화하고 8대 도메인 등으로 분류하여 보여주는 방식은 구식 시각화 도구(AOI) 수준의 단계입니다.
- **BR (통제 규칙):** 데이터가 지켜야 할 업무 규칙(Business Rules)을 기계적으로 추출하고, 이를 시스템에 강제하는 자동화된 거버넌스를 지향합니다.
- **목표:** 관리자의 주관을 배제하고, 수치적 지표(엔트로피, 신뢰도)에 근거하여 기계가 스스로 규칙을 생성하고 우선순위를 결정하는 지능형 시스템 구축입니다.

### **2. 기술적 파이프라인 (자동화 로직)**

- **변수 선택 (Entropy):** 사람이 분석 대상을 정하지 않고, 엔트로피 계산을 통해 정보량이 높은 속성을 자동으로 추출하여 분석 범위를 확정합니다.
- **데이터 이산화 (Discretization):** 수치 데이터 자체는 분석 대상에서 제외하며, AOI를 활용하여 수치를 범주형 계층($L_0 \sim L_4$)으로 변환(Roll-up)하여 트랜잭션 데이터화합니다.
- **패턴 마이닝 (FP-tree):** 다차원(Multi-dimensional) 공간에서의 동시 출현 패턴을 탐사하기 위해 FP-tree 알고리즘을 가동합니다.
- 
    
    **규칙 정형화 (PBR):** 탐사된 최빈발 패턴을 `IF-THEN` 구조의 업무 규칙(Pattern-Based Rule)으로 변환하여 룰 엔진에 자동 등록합니다.
    

### **3. 서브스페이스(Subspace) 집중 분석**

- **범위 한정:** 전체 DB 대신 시간, 공간, 관계가 명확한 특정 도메인(예: 대중교통)을 잘라 분석하여 신뢰도를 확보합니다.
- **지하철 예시:** '역 코드-노선-시간' 사이의 연관 관계를 분석하여, 초단위 데이터에서 연결되는 테이블 군집과 정합성 규칙을 도출합니다.

### **4. 거버넌스 실행 방안**

- **전체 결합지도 (Full Joint Map):** FK/PK 관계를 수치로 연결하고 피보팅(Pivoting)하여 1,000여 개 테이블의 시퀀스 연계망을 구축합니다.
- **우선순위 관리 (Ranking):** 연결 밀도(Connectivity)와 사용 빈도가 높은 '대표 컬럼' 및 '대표 테이블'을 거버넌스 1순위 관리 대상으로 자동 선정합니다.
- **품질 지표화:** 각 규칙이 데이터의 몇 퍼센트($\%$)를 커버하는지 신뢰도 확률을 나열하여 관리자의 의사결정 근거를 수치로 제시합니다.

### **5. 요약: 최종 산출물의 구성**

| **구분** | **내용** | **비고** |
| --- | --- | --- |
| **분석 도구** | 엔트로피, FP-tree, 이산화(AOI) | 기계적 자동화 도구 |
| **분석 대상** | 대중교통 등 특정 서브스페이스 | 신뢰도 확보 목적 |
| **최종 성과 (BR)** | 신뢰도($\%$) 기반 업무 규칙 리스트 | 지능형 거버넌스 룰셋 |
| **거버넌스 액션** | 마스터 노드 및 품질 교정 우선순위표 | 실무 가이드라인 |

이 전략은 '지도는 그냥 DB'라는 전제하에, 데이터 지도에서 기계적으로 후보군을 찾고 수치로 최종 통제 규칙을 결정하는 체계입니다. 추가적인 수치 수정이나 예시 보완이 필요하시면 말씀해 주세요.

교수님께서 강조하신 '전체 결합지도(Full Joint Map)'와 '거버넌스 자동화'를 중심으로, 현재 프로젝트에서 확장할 수 있는 데이터 거버넌스의 주요 방안들을 정리해 드립니다.

교수님의 핵심 메시지는 단순히 데이터를 분석하는 것을 넘어, "데이터 사이의 연결고리를 찾아내고, 이를 기반으로 무엇을 먼저 관리할지(우선순위)를 기계적으로 결정하는 체계를 세우는 것"입니다.

---

### **1. FK/PK 기반의 전체 결합지도(Full Joint Map) 구축**

가장 직접적으로 언급하신 방안으로, 파편화된 테이블들을 하나의 거대한 연계망으로 시각화하는 방법입니다.

- **방법:** 테이블 간의 FK(외래키)와 PK(기본키) 관계를 추적하여, 수천 개의 테이블이 어떻게 시퀀스로 연결되는지 피보팅(Pivoting)하여 결합합니다.
- **거버넌스 가치:** 개별 테이블 단위의 관리가 아니라, 데이터의 흐름(Lineage)을 파악할 수 있습니다. 예를 들어 "A 테이블의 주소 데이터가 바뀌면 연결된 100개의 테이블에 어떤 영향을 주는가?"를 한눈에 볼 수 있는 '데이터 혈통 지도'가 됩니다.

### **2. 우선순위 기반의 품질 관리 (Priority Ranking)**

"표준화가 안 됐다"라고 말만 하는 것이 아니라, **어떤 데이터를 가장 먼저 고쳐야 할지 순위를 매기는 것**이 거버넌스의 핵심입니다.

- **방법:** 다음의 지표를 결합하여 우선순위를 자동 산출합니다.
    - **연결 밀도:** FK/PK로 다른 테이블과 가장 많이 연결된 '대표 테이블'과 '대표 컬럼'.
    - **사용 빈도:** 실제 시스템에서 가장 자주 호출되거나 참조되는 데이터.
    - 
        
        **오류 심각도:** 엔트로피나 빈발 패턴에서 벗어난 정도(Outlier score).
        
- **거버넌스 가치:** 한정된 리소스로 최대의 품질 개선 효과를 낼 수 있는 '거버넌스 의사결정 자동화'를 실현합니다.

### **3. 빈발 패턴 기반의 지능형 업무 규칙(BR) 수립**

"이미 알고 있는 것"이 아니라, 데이터 스스로가 말하는 **의외의 규칙**을 찾아내어 시스템에 적용하는 방법입니다.

- 
    
    **방법:** **FP-tree** 알고리즘을 통해 다차원(Multi-dimensional) 공간에서 동시에 발생하는 속성 조합을 찾고, 이를 `IF-THEN` 형태의 PBR(Pattern-Based Rule)로 정형화합니다.
    
- **거버넌스 가치:** "지하철 승차 시각이 있으면 반드시 역 코드가 존재해야 한다"와 같은 규칙을 추출하고, 이 규칙의 신뢰도(%)를 산출하여 사람이 개입하지 않는 **자동 검증(Validation)** 체계를 구축합니다.

### **4. 롤업(Roll-up) 및 개념 계층화 관리**

데이터를 너무 잘게 보지 말고, 상위 개념으로 묶어서 패턴을 명확히 하는 방법입니다.

- **방법:** 개별 테이블이나 컬럼 단위를 넘어 **'기관' 단위나 '도메인 그룹' 단위로 데이터를 롤업**하여 분석합니다.
- **거버넌스 가치:** 로우 레벨에서는 보이지 않던 전역 표준 도메인(Global Standard Domain)의 특성을 발견할 수 있으며, 범주화된 지표를 통해 거시적인 데이터 정책을 세울 수 있습니다.

---

### **💡 결론: BI에서 BR로의 전환 (Automation & Intelligence)**

교수님께서 제시하신 이 모든 방안의 종착점은 '자동화된 지능형 거버넌스'입니다.

1. **BI (Business Intelligence):** "현재 데이터 상태가 이렇다"라고 묘사하고 시각화하는 단계 (현재의 한계).
2. **BR (Business Rules):** "데이터는 반드시 이 규칙을 따라야 하며, 위반 시 1순위로 교정한다"라고 **기계적으로 통제하는 단계** (우리의 목표).

따라서 발표 자료에는 "우리가 지도를 그렸다"는 내용보다, "이 지도를 통해 어떤 PK/FK 연결 고리를 찾았고, 그중 가장 중요한 데이터(Priority 1)에 대해 기계가 어떤 룰(BR)을 스스로 찾아냈는지"를 보여주는 것이 가장 강력한 보완 방법이 될 것입니다.