# 데이터지도 거버넌스·인텔리전스 탐사 — 핵심 정리

> 공공데이터포털(Data.go.kr) **999개 데이터셋**의 정밀 "데이터 지도"(프로파일링 메타데이터)를
> **비지도 데이터마이닝**으로 분석하여 ① 데이터 거버넌스 이슈와 ② 비즈니스 인텔리전스(메타 수준 연관규칙)를 도출한 프로젝트.
>
> 본 문서는 `proj1.PNG`·`proj2.PNG`(과제 지침), `scripts/`(분석 코드), `report/`(PPT 산출물), `analysis/`(근거 리포트)의 **중요 내용을 한 장으로 압축**한 요약본입니다.
>
> 🤝 **작업을 이어받는 경우(다른 PC/세션)** → 먼저 [`CLAUDE.md`](CLAUDE.md)(환경·PPT 안전 편집 방법론·슬라이드↔파일 매핑·버전 히스토리). 최신 보고서 = **V12**(20장).

---

## 0. 과제 지침 (proj1 / proj2 원본)

> ⚠️ **정정**: 기존 `README.md`·`analysis/CANDIDATES.md`는 두 PNG를 "내용이 전부 0인 손상 파일"로 기록했으나, 현재 두 파일은 **정상적으로 판독**됩니다. 아래는 그 실제 지침입니다.

### proj2 — (1) 데이터 플랫폼의 데이터 거버넌스 관련 지식 탐사
- 데이터 플랫폼의 데이터에 대한 **정밀 데이터 지도**를 이용, 데이터 거버넌스 지능화를 위한 인사이트 지식 탐사
- 제공 데이터: **공공데이터포털(Data.go.kr)** 정밀 데이터 지도 (**4개 카테고리, 1000개 테이블**)
- **① 컬럼 도메인/값 관련 거버넌스 특성 인사이트**
  - 컬럼 도메인/값 정합성 측정 및 거버넌스 규칙 탐사
  - 컬럼 도메인/값 연관성 탐사
  - 카테고리 계층구조의 공통/대표 도메인 발견 및 도메인 사전 표준화
- **② 단일/연계 테이블 특성 인사이트**
  - 마스터 테이블 & 임시 데이터, 테이블 연관성 발견
  - PK&FK 연계 테이블 거버넌스 지식 탐사
  - 단일/연계 테이블 거버넌스 업무 규칙(Business Rules) 발견
  - 카테고리 계층구조의 테이블 분류 정합성 측정/검증 방안 도출

### proj1 — 비지도 학습 알고리즘 Only (제약)
- 사용 기법: Concept Description & Comparison(AOI), 군집(1-D & Subspace Clustering), 빈발패턴(Frequent Patterns), 연관규칙(Association), 순차패턴(Sequential Patterns)
- **(필수)** 최소 2개 이상 비지도 알고리즘 **연계** · 최소 2개 이상 **다계층(Multi-level)** 분석 → *The more, the better*
- **(감점)** ❌ 지도학습(분류 등) 금지, ❌ **LLM 추론 금지**
- **(평가지표)** 단계별 보고서 · 분석 목표/과정의 명확성 · 결과의 정확성 검증 및 효용성 진단

> **핵심 제약 준수**: 본 프로젝트의 모든 결론은 결정적(deterministic) 비지도 알고리즘 실행 결과이며 스크립트로 100% 재현 가능. LLM은 추론에 사용하지 않음.

---

## 1. 대상 데이터 (`data/`, UTF-8 CSV)

도메인 분포: **교육 400 · 보건 300 · 공공행정 299** (cate1) / 중분류(cate2) 6종 각 100~300건.
원본 트랜잭션이 아니라 1,000개 데이터셋을 프로파일링한 **3계층 메타데이터(데이터 지도)**.

| 파일 | 역할 | 규모 |
|---|---|---|
| `table_map.csv` | 데이터셋 1건당 메타 + 분류(cate1/cate2) + 통계 | 999행 |
| `column_map.csv` | 컬럼 1건당 프로파일(distinct_cnt, pk_ratio, same_ratio …) | 9,840행 |
| `distinct_value.csv` | 컬럼별 distinct 값 + 빈도 (값 수준) | 803만행 · 635MB |
| `table_summary_map` / `column_summary_map` | 타 객체와의 중첩 통계 롤업 | 996 / 8,360행 |
| `combined_pair_map.csv` | 컬럼 쌍 조인 가능성(linking_ratio) | 74만행 · 300MB |
| `table_pair_map.csv` | 테이블 쌍 유사도 | 25만행 · 296MB |
| `column_pair_map.csv` | 컬럼 쌍 값/도메인 중첩 | 524만행 · 3.4GB |
| `*_value_tree` / `*_str_tree` | 값/이름 유사도 계층 클러스터(덴드로그램) | 331~4,549행 |

---

## 2. 분석 방법론 (6단계 파이프라인)

```
원자료 ─►[이산화]─►[AOI 롤업]─►[트랜잭션 구성]─►[빈발패턴]─►[연관규칙]─►[국소→전체 검증]
        (1D KMeans) (개념계층)   (테이블=거래)   (FP-Growth)  (lift 등)    (규칙#4)
```

| 단계 | 알고리즘 | 적용 |
|---|---|---|
| ② 수치 이산화 | `log1p` + **1D KMeans** | rec_cnt·column_cnt·수치비 → 순서형 구간(극소~극대) |
| ③ AOI 롤업 | 정규식 개념계층 | 999개 기관명 → 7개 기관유형 / col_nm → 정규화 컬럼개념 |
| ④ 트랜잭션 | TransactionEncoder | 테이블 1건 = 1 거래, 속성·컬럼개념 = 아이템 |
| ⑤ 빈발/연관 | **FP-Growth** + association_rules | support·confidence·**lift**·leverage·conviction |
| ⑥ 검증 | 도메인 국소 채굴 → 전체 999 재계산 | 규칙#4(국소→전체 일반성 판정) |

**이산화 경계(KMeans, 설명가능)**
- 레코드수: 극소[1–22]314 · 소[23–130]260 · 중[133–829]224 · 대[843–8,019]128 · 극대[8,578–833,466]73
- 컬럼폭: 협소[1–5]381 · 표준[6–11]443 · 광폭[12–44]162 · 초광폭[49–338]13
- 기관유형(AOI): 기초지자체404 · 공공기관245 · **미상(소스)145** · 광역지자체83 · 교육기관49 · 중앙행정39 · 기타34

---

## 3. 주요 발견 ①: 데이터 거버넌스 이슈

### 🛡️ G1. 출처 결손의 **계통성** (거버넌스 헤드라인)
- `table_source`가 placeholder **"소스"**인 데이터셋 **145건(14.5%)**.
- 결정적으로, **`유아및초·중등교육` 분류 100건이 100% placeholder** (lift 6.89, conf 1.00).
- → 무작위 누락이 아닌 **단일 수집 배치의 계통적 provenance 손실**. **복구 우선순위 1순위.**

| cate2 | placeholder | 총 | 비율 |
|---|---:|---:|---:|
| **유아및초·중등교육** | **100** | 100 | **100%** |
| 교육일반 | 15 | 100 | 15% |
| 고등교육 | 14 | 100 | 14% |
| 보건의료 | 10 | 300 | 3% |

### 🛡️ G2. "죽은 메트릭" — 데이터 지도 자체의 미충전 지표
- `null_ratio` · `table_qty_index` · `cum_search_num`이 **9,840개 컬럼 전부 0**.
- → 카탈로그가 품질/활용 지표를 산출 못 함(적재 실패). 품질·활용 축 분석 불가.

### 🛡️ G3. 컬럼명 표준 부재 (동일 개념·다른 이름)
- 날짜: `데이터기준일자`(169) vs `데이터기준일`(46) vs `등록일`(35)
- 일련번호: `연번`(168) vs `순번`(79) vs `번호`(34) / 연도: `연도`(62) vs `년도`(26)
- → 마스터 컬럼 사전 부재. 문자유사도 군집(G3 후속)으로 표준어 사전 도출 권장.

### 🛡️ G4·G5·G6. 키 품질 / 스키마 정합성 / 네이밍 오류
- 상수 컬럼(distinct=1, 정보량 0) **1,372개(13.9%)**.
- `column_cnt ≠ categorical_cnt + numerical_cnt`인 컬럼 회계 불일치 테이블 **56개**.
- 메타 네이밍 오류: **`same_ratio`는 비율이 아니라 평균중복도(rec/distinct)** — 컬럼명 오해 소지.

---

## 4. 주요 발견 ②: 비즈니스 인텔리전스 ("주말>기저귀>맥주" 대응 규칙)

테이블=거래, 보유 컬럼개념=아이템으로 한 메타 수준 연관규칙(전체 999, lift>2).

| 주제 | 규칙 | #테이블 | conf | lift |
|---|---|---:|---:|---:|
| 지리좌표쌍 | 경도 → 위도 (3개 도메인 공통 POI 지문) | 44 | 1.00 | 22.7 |
| 교육스키마 | 교과목코드 ↔ 분반 | 8 | 1.00 | 124.9 |
| 사학재정 | 연구비 → {일반기부금, 지정기부금} | 8 | 1.00 | 111.0 |
| 기간쌍 | 교육시작일 → 교육종료일 | 10 | 1.00 | 90.8 |
| 의료기관 PII | 의료기관전화번호 → {명, 주소} (**재식별 위험 묶음**) | 15 | 0.88 | 46.4 |
| 행정구역 계층 | 시도명 → 시군구명 (공간 롤업 축) | 15 | 0.94 | 39.0 |
| 인구통계 | 연령대 → 성별 | 11 | 0.85 | 35.2 |

**읽는 법**: "데이터셋에 *경도*가 있으면 거의 확실히 *위도*도 있고(conf 1.0), 그 동반은 우연 대비 22.7배(lift) 흔하다." → 시각화 가능 POI 데이터셋 식별 신호.

### 스키마 패밀리 (최대빈발 컬럼군 = 데이터셋 원형)
- **사학재정군** {법인명, 연구비, 기부금, 학교명…} — 한국사학진흥재단 8건 (통합 후보)
- **의료기관군** {순번, 의료기관명, 전화번호, 주소} — PII 거버넌스 표준 적용 단위
- **POI군** {경도, 위도, 도로명주소, 전화번호} — **3개 도메인 가로지르는 범용 템플릿** → 표준 데이터모델 정의 대상
- **국가시험군** {연도, 직종, 회차} — 한국보건의료인국가시험원 9건

---

## 5. 주요 발견 ③: 규칙#4 검증 (국소→전체)

도메인 내부에서 캔 강규칙을 전체 999건에서 재계산하여 일반성 판정. **핵심 교훈 = 비대칭/약화 사례.**

| 규칙 | 공공행정 국소 conf/lift | 전체 conf/lift | 판정 |
|---|---|---|---|
| 소재지지번주소 → 소재지도로명주소 | 1.00 / 42.7 | 0.92 / 36.6 | **유지** |
| 소재지도로명주소 → 소재지지번주소 | 1.00 / 42.7 | **0.44** / 36.6 | **약화** (역방향 비성립) |
| 경도 → 위도 | 1.00 / 29.9 | 1.00 / 22.7 | 유지(lift 하락) |

→ 공공행정에선 도로명·지번이 1:1 병기되나, **보건은 지번 단독 보유**가 많아 전체에서 도로명→지번이 깨짐. **국소 강규칙을 전체로 일반화하면 오류**가 됨을 실증.
→ 부수 발견: 보건 일부 데이터셋은 실데이터가 아닌 **데이터 명세서/사전 파일**(카탈로그 오분류 후보).

---

## 6. 산출물 & 재현

| 구분 | 위치 |
|---|---|
| 분석 코드(번호순 실행) | `scripts/01~09_*.py` (프로파일→기초분석→규모/교차도메인→FP-Growth→보강→PPT/차트) |
| 근거 리포트 | `analysis/I1_REPORT.md`(⭐메인), `CANDIDATES.md`, `basic_analysis.md`, `I1_metrics_detail.md` |
| 최종 보고서 | `report/데이터지도_분석보고서.pptx` (21장·16:9) + `report/figs/*.png`(차트 6종) |
| 이론 배경 | `analysis/THEORY_이론배경.md` |

**환경**: Python 3.10+ / `pandas numpy scikit-learn scipy mlxtend` (KMeans 경고 억제 `OMP_NUM_THREADS=4`)

---

## 7. 한계 & 다음 단계

**한계**
- 컬럼개념은 결정적 정규화만 적용, **동의어 병합(연번≈순번) 미적용**(BI 규칙 오염 방지).
- 장바구니 규칙 support 낮음(8~44 테이블) → lift 높아도 희소, 운영 적용 전 도메인 확인 필요.
- `table_qty_index·cum_search_num·null_ratio` 전부 0(미충전) → 활용/품질 축 분석 불가.
- `distinct_value.csv`는 정렬되어 앞부분 샘플 편향 → 전체검증 필수.

**다음 단계 후보**
- **I3** — POI/의료기관 패밀리를 `combined_pair_map.linking_ratio`로 실제 조인키(PK/FK) 검증.
- **G3** — 동의어(연번/순번/번호…) 문자유사도 군집 → 표준어 사전.
- **I5** — 컬럼 프로파일 서브스페이스 클러스터링으로 컬럼 원형(archetype) 정교화.
